# Server-side base URL for the Next BFF proxy (/api/desk/* -> FastAPI). Read by `next dev` / `next
# start` (and the Docker image); the browser never sees it. Defaults to http://127.0.0.1:8000.
API_BASE=http://127.0.0.1:8000
