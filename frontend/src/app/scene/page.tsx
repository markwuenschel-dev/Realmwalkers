import SceneScreen from "../../desk/screens/SceneScreen";

// Pending review queue. A focused, out-of-queue scene lives at /scene/[sceneId].
export default function Page() {
  return <SceneScreen />;
}
