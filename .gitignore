__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
.env
.env.*
!.env.example
frontend/node_modules/
frontend/dist/
.DS_Store
.vscode/
.idea/
*.lnk
# runtime exhaust — never tracked (DESIGN: keep the tree clean)
logs/
*.log
# local agent tooling — .claude/skills are machine-specific symlinks into .agents/skills/ (the tracked home)
# .cursor/skills/ is NOT gitignored: Cursor skips gitignored paths when loading the / slash menu.
# Populate locally after pull: bash scripts/sync-cursor-skills.sh
.claude/skills/
# author's local working notes — scratch, never tracked
author notes/
