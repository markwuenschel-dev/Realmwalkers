# SceneFidelity hard-clause criteria

Every hard Fidelity Clause has exactly one typed Satisfaction Criterion with a closed evidence kind and non-empty author-readable statement. One criterion may cite multiple prose anchors; multiple observable claims must be split into dependent clauses. Active hard clauses missing a valid criterion are deterministic packet-approval blockers, while standard clauses may omit one.
