# SceneFidelity explicit activation

Only author-accepted `fidelity_requirements` in a ScenePacket are active. Heuristics and packet-author output may create Fidelity Suggestions, but suggestions do not reach the drafter, create Critiques or Issues, or affect export readiness. This includes `reader_movie`, preventing SceneFidelity from becoming universal taste-based QA.
