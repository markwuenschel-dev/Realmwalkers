# SceneFidelity clause identity

Each active SceneFidelity Requirement has a stable server-minted `requirement_id`, and each atomic preservation claim within it has a stable server-minted `clause_id`. Evaluation, repair, and override records identify the exact clause rather than a mutable array position or requirement-level summary.
