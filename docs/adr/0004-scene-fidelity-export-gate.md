# SceneFidelity post-draft policy

Every active SceneFidelity Requirement must pass its typed deterministic validator before ScenePacket approval and drafting. Its `post_draft_policy` is then either `advisory`, which creates warnings only, or `export_required`, which may create production repair work that holds Production Run completion and final chapter export until resolved or author-overridden. This policy does not prevent Scene author approval.
