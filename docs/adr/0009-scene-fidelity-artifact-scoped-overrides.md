# SceneFidelity artifact-scoped overrides

Only the author may override a SceneFidelity repair Issue. The override records its reason, evaluated artifact, and affected clauses; it does not mutate the report or inherit to later DraftAttempts. Changing the requirement itself requires a ScenePacket edit and approval.
