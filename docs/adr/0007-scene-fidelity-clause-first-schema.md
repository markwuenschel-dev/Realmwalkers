# SceneFidelity clause-first schema

Active requirements use one `clauses` collection of structured, clause-identified objects instead of parallel `must_preserve` and `hard_preserve` text lists. Each clause declares `standard` or `hard` enforcement, allowing policy, repair, and overrides to address one stable contract claim.
