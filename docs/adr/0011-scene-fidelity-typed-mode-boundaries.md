# SceneFidelity typed mode boundaries

SceneFidelity has a closed registry of five typed modes: relationship turn, intimacy blocking, combat blocking, spatial affordance, and reader movie. Each mode validates and evaluates its own contract shape; each clause belongs to exactly one mode. Modes may compose, but they reference related facts rather than duplicate obligations, and `reader_movie` remains perceptual rather than a universal quality review.
